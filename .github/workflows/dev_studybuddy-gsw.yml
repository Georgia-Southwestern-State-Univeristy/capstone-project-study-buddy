name: Build and Deploy Angular App to Azure Web App - studybuddy-Gsw

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      - name: 🌐 Install Angular CLI
        run: npm install -g @angular/cli

      - name: 📦 Install deps and build Angular app
        run: |
          cd meme_mingle
          npm install
          ng build --configuration production

      - name: 🗂️ Prepare deployment package
        run: |
          mkdir deploy
          cp meme_mingle/server.js deploy/
          cp meme_mingle/package.json deploy/
          cp -r meme_mingle/dist/meme-mingle deploy/dist

      - name: 🗜️ Zip deploy folder
        run: |
          cd deploy
          zip -r ../release.zip .

      - name: ☁️ Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: angular-dist
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: 📥 Download artifact
        uses: actions/download-artifact@v4
        with:
          name: angular-dist
          path: .

      - name: 🗜️ Unzip artifact
        run: unzip release.zip

      - name: 🚀 Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'studybuddy-Gsw'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_A78A924CE36A49C5BBB15A3DC10C2BDC }}
          package: '.'

      - name: 🔄 Restart App Service
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az webapp restart --name studybuddy-Gsw --resource-group $(az webapp show --name studybuddy-Gsw --query resourceGroup --output tsv)